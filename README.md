# HabitFlow 🌱

> Tu tracker semanal de hábitos — construye la mejor versión de ti mismo.

## ✨ Funcionalidades

- ✅ Tabla semanal (Lun–Dom) × hábitos con checkboxes interactivos
- 💾 Persistencia automática en `localStorage`
- 📊 Score semanal con barra de progreso y anillo circular animado
- 🧠 Análisis qualitativo con insights personalizados
- 📅 Historial de semanas anteriores
- ➕ Agregar / editar / eliminar hábitos con selector de emojis
- 🎉 Confetti y micro-animaciones al completar hábitos
- 🌙 Dark mode premium con gradientes índigo/violeta
- 📱 Completamente responsive

## 🚀 Deploy en Vercel

### Opción 1 – GitHub + Vercel (recomendada)

```bash
# 1. Inicializa git y sube a GitHub
git init
git add .
git commit -m "feat: HabitFlow inicial"
git remote add origin https://github.com/TU_USUARIO/habitflow.git
git push -u origin main
```

Luego entra a [vercel.com](https://vercel.com), conecta tu repositorio y haz clic en **Deploy**. ¡Listo!

### Opción 2 – Vercel CLI

```bash
npm i -g vercel
vercel --prod
```

## 🛠 Desarrollo local

Abre `index.html` directamente en tu navegador — no requiere servidor.

## 📁 Estructura

```
radiant-curie/
├── index.html    # App completa (HTML + CSS + JS)
├── vercel.json   # Config de deploy
└── README.md
```
